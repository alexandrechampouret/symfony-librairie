<form method="post" action="{{ path('admin_author_delete', {'id': author.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author.id) }}">
    <button class="btn btn-danger mt-3"><i class="align-middle" data-feather="trash-2"></i></button>
</form>
