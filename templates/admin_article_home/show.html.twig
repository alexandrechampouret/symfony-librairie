{% extends 'base-admin.html.twig' %}

{% block title %}ArticleHome{% endblock %}

{% block body %}
    <h1>ArticleHome</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_home.id }}</td>
            </tr>
            <tr>
                <th>Article</th>
                <td>{{ article_home.article }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_article_home_index') }}">back to list</a>

    <a href="{{ path('admin_article_home_edit', {'id': article_home.id}) }}">edit</a>

    {{ include('admin_article_home/_delete_form.html.twig') }}
{% endblock %}
