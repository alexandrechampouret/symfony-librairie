<form method="post" action="{{ path('admin_article_home_delete', {'id': article_home.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_home.id) }}">
    <button class="btn">Delete</button>
</form>
